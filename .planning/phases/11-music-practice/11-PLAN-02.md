---
wave: 1
depends_on: []
files_modified:
  - ui/src/store/workspace.store.ts
  - ui/src/store/layout.store.ts
  - ui/src/panels/layout_presets.ts
  - ui/src/panels/PanelManager.tsx
  - ui/src/App.tsx
autonomous: true
requirements:
  - WorkspaceId union includes 'music'
  - Alt+6 shortcut navigates to music workspace
  - Music preset defined with 3 panels: pitch-roll, tuner, session-log
  - LAYOUT_VERSION bumped from 2 to 3
  - ALL_WORKSPACES includes 'music'
  - WORKSPACE_PANELS includes MusicWorkspace lazy import

must_haves:
  truths:
    - "Pressing Alt+6 calls navigate('music') in App.tsx"
    - "WorkspaceId type accepts 'music' without TypeScript errors"
    - "NEURAL_MAP_MODES['music'] resolves to 'provenance'"
    - "ALL_WORKSPACES includes 'music' — localStorage cleanup on LAYOUT_VERSION change covers the music key"
    - "LAYOUT_VERSION is 3 — forces preset reset for all existing users"
    - "getPreset('music') returns 3 PanelState entries with ids pitch-roll, tuner, session-log"
    - "PanelManager lazy-imports MusicWorkspace and includes it in WORKSPACE_PANELS"
  artifacts:
    - path: "ui/src/store/workspace.store.ts"
      provides: "WorkspaceId union with 'music', NEURAL_MAP_MODES entry"
    - path: "ui/src/store/layout.store.ts"
      provides: "ALL_WORKSPACES with 'music'"
    - path: "ui/src/panels/layout_presets.ts"
      provides: "music preset with 3 panels, LAYOUT_VERSION = 3"
    - path: "ui/src/panels/PanelManager.tsx"
      provides: "MusicWorkspace lazy import + WORKSPACE_PANELS entry"
    - path: "ui/src/App.tsx"
      provides: "Alt+6 keyboard shortcut"
  key_links:
    - from: "App.tsx keyboard handler"
      to: "useWorkspaceStore.navigate"
      via: "if (e.altKey && e.key === '6') navigate('music')"
    - from: "PanelManager WORKSPACE_PANELS['music']"
      to: "ui/src/workspaces/music/index.tsx"
      via: "lazy(() => import('../workspaces/music'))"
---

# Plan 02: UI Type Wiring — Stores, Presets, Router

## Objective

Wire the music workspace into the existing React workspace system: extend the TypeScript union, register the layout preset with correct geometry, add the Alt+6 shortcut, and lazy-import the forthcoming MusicWorkspace component. This plan does not create the music component itself — that is Plan 04. The lazy import will produce a TypeScript module-not-found error until Plan 04 creates the file, but the build still compiles (lazy imports are only resolved at runtime).

This plan has zero Python dependencies — it can execute in parallel with Plan 01.

## Tasks

<task id="T1" title="Extend workspace.store.ts and layout.store.ts">
<read_first>
- d:\Zuki\ui\src\store\workspace.store.ts
- d:\Zuki\ui\src\store\layout.store.ts
</read_first>
<action>
workspace.store.ts — two changes:

1. On line 3, change the WorkspaceId union from:
     export type WorkspaceId = 'broker' | 'business' | 'coding' | 'os' | 'office'
   to:
     export type WorkspaceId = 'broker' | 'business' | 'coding' | 'os' | 'office' | 'music'

2. In the NEURAL_MAP_MODES Record (lines 17–23), add a new entry:
     music: 'provenance',
   The full record becomes: broker, business, coding, os, office, music — all mapped. Use 'provenance' for music (same as coding — dependency flow visualization is the closest semantic match).

layout.store.ts — one change:

3. On line 27, change ALL_WORKSPACES from:
     const ALL_WORKSPACES = ['broker', 'business', 'coding', 'os', 'office']
   to:
     const ALL_WORKSPACES = ['broker', 'business', 'coding', 'os', 'office', 'music']

No other changes to either file. Do not change store logic, interfaces, or imports.
</action>
<acceptance_criteria>
- workspace.store.ts line 3 contains | 'music'
- workspace.store.ts NEURAL_MAP_MODES contains music: 'provenance'
- layout.store.ts ALL_WORKSPACES contains 'music'
- Running: cd d:\Zuki\ui && npx tsc --noEmit 2>&1 | Select-String "workspace.store" produces no errors related to WorkspaceId
</acceptance_criteria>
</task>

<task id="T2" title="Add music preset to layout_presets.ts and bump LAYOUT_VERSION">
<read_first>
- d:\Zuki\ui\src\panels\layout_presets.ts
</read_first>
<action>
Two changes to layout_presets.ts:

CHANGE 1 — Bump LAYOUT_VERSION on line 6:
  export const LAYOUT_VERSION = 2
→ export const LAYOUT_VERSION = 3

CHANGE 2 — Add music key to the object returned by the preset() function. Insert after the os block (before the closing brace of the return statement):

  music: [
    AVATAR, NEURAL, TERMINAL,
    { id: 'pitch-roll',   x: CX, y: 16,            w: CW, h: SAFE - 16,           collapsed: false, zIndex: 5 },
    { id: 'tuner',        x: RX, y: NY,             w: RW, h: 160,                 collapsed: false, zIndex: 6 },
    { id: 'session-log',  x: RX, y: NY + 160 + 8,  w: RW, h: Math.max(0, RH - 168), collapsed: false, zIndex: 6 },
  ],

Geometry rationale (using existing shared constants CX, CW, RX, RW, NY, RH, SAFE):
- pitch-roll: full centre column from y=16 to SAFE — the main visualization panel, needs maximum vertical space
- tuner: right column, top position (at NY, below the neural map), fixed height 160px — compact display for note name + cents
- session-log: right column, below tuner with 8px gap, remaining right-column height — scrollable event stream

No other changes. Do not modify existing workspace presets (broker, business, coding, office, os).
</action>
<acceptance_criteria>
- layout_presets.ts contains export const LAYOUT_VERSION = 3
- layout_presets.ts contains id: 'pitch-roll'
- layout_presets.ts contains id: 'tuner'
- layout_presets.ts contains id: 'session-log'
- layout_presets.ts music array includes AVATAR, NEURAL, TERMINAL entries
- Running getPreset('music') in browser devtools returns an array of 6 PanelState objects (3 shared + 3 workspace-specific)
</acceptance_criteria>
</task>

<task id="T3" title="Register MusicWorkspace in PanelManager.tsx and add Alt+6 in App.tsx">
<read_first>
- d:\Zuki\ui\src\panels\PanelManager.tsx
- d:\Zuki\ui\src\App.tsx
</read_first>
<action>
PanelManager.tsx — two changes:

1. Add lazy import after the existing OfficeWorkspace import (line 15):
   const MusicWorkspace = lazy(() => import('../workspaces/music'))

2. Add entry to WORKSPACE_PANELS Record (after the office entry):
   music: MusicWorkspace,

The WORKSPACE_PANELS object should now have 6 entries: broker, business, coding, os, office, music.

App.tsx — one change:

3. In the keyboard handler useEffect, after the line:
     if (e.altKey && e.key === '5') navigate('office')
   add:
     if (e.altKey && e.key === '6') navigate('music')

Do not add e.preventDefault() for Alt+6 — existing Alt+1 through Alt+5 shortcuts do not call preventDefault either (verified in source). Keep consistent.

Do not modify any other logic in either file.
</action>
<acceptance_criteria>
- PanelManager.tsx contains const MusicWorkspace = lazy(() => import('../workspaces/music'))
- PanelManager.tsx WORKSPACE_PANELS contains music: MusicWorkspace
- App.tsx contains if (e.altKey && e.key === '6') navigate('music')
- cd d:\Zuki\ui && npm run build exits 0 (lazy import module-not-found is a runtime error, not build-time — Vite does not resolve lazy imports at build)
</acceptance_criteria>
</task>

## Verification

From d:\Zuki\ui run:

  npm run build

Build must exit 0. TypeScript type errors in workspace.store.ts or PanelManager.tsx (WorkspaceId unknown) must not appear.

Manual spot-check after Plan 04 is complete: Press Alt+6 in browser — workspace transitions to music, neural map shows provenance mode, layout loads with 3 panels in correct positions.

## must_haves

- WorkspaceId union includes 'music' — no TypeScript errors on navigate('music')
- NEURAL_MAP_MODES['music'] = 'provenance'
- ALL_WORKSPACES includes 'music' for localStorage cleanup
- LAYOUT_VERSION = 3 (was 2) — forces all users to get fresh panel positions
- music preset: 6 panels (AVATAR, NEURAL, TERMINAL + pitch-roll, tuner, session-log)
- PanelManager lazy-imports MusicWorkspace from '../workspaces/music'
- Alt+6 keyboard shortcut wired in App.tsx
